# Core dependencies for robotron-rl training
#
# NOTE: NumPy 1.26.x is intentionally used (not 2.x) for compatibility
# with Stable Baselines3 and other ML libraries that don't support NumPy 2.x yet.

# NumPy - use 1.26.x, not 2.x
numpy>=1.26.0,<2.0.0

# Gymnasium - use 1.x
gymnasium>=1.0.0
opencv-python>=4.10.0  # Required by ResizeObservation wrapper

# PyTorch
torch>=2.4.0

# Reinforcement Learning
stable-baselines3>=2.3.0
sb3-contrib>=2.3.0

# Logging and monitoring
wandb>=0.18.0
tensorboard>=2.18.0

# Utilities
pyyaml>=6.0.0
psutil>=6.0.0

# The robotron2084gym environment should be installed separately:
# pip install -e robotron2084gym/
